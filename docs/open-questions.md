# Open questions & known limitations

Items that are unclear, partially implemented, not yet confirmed with stakeholders, or need external verification. Check here before assuming — and add here instead of guessing silently when a task surfaces something unconfirmed.

---

## API gaps the console works around (opened Phase 5a)

Each of these is a capability the console needs and the API does not currently provide. None is a defect: the API is correct as specified, and the console has a working — sometimes visibly compromised — answer for each. They are recorded so the compromise is a decision on the record rather than an accident, and so Phase 6 has a shortlist.

| # | Item | Status | Action needed |
|---|---|---|---|
| 1 | **No procedure returns the caller's role.** *(Phase 5b: worked around client-side; see `docs/adr/0009-console-session-and-tenant-model.md`.)* `role` is derived inside `requireOrg` and lives only in middleware context (`packages/api/src/procedures.ts`). The console cannot ask the API "am I an admin here?" | By design, with a client-side workaround | Console now imports the same `toLedgerRole` from `packages/api/auth/roles` (no duplicated mapping). Still an affordance hint only; every write still handles `403 insufficient_role`. A role-returning read would remove the Better Auth member round-trip |
| 2 | ~~**`transactions.list` returns no amounts and no postings.**~~ **Resolved, Phase 6b.** It now returns `transactionWithPostingsSchema`, and the history table shows what moved | **Resolved 2026-07-28** | **The N+1 objection recorded here was about the wrong actor.** It said the alternative "fans out up to 200 membership-checked requests per page" — true of the *client* calling `transactions.get` per row, which is what was being rejected. Server-side it is **one** extra query: page the transactions, then a single `WHERE transaction_id IN (…)` covered by `ledger_posting_transactionId_idx`, grouped in memory. Two queries per page, constant in page size, pinned by a test that wraps `pool.query` and compares the count for a 6-row page against a 2-row page. No scalar `amount` field was added: a balanced transaction may have any number of legs, so a split has no single amount, and the console sums the debits instead (`features/transactions/total.ts`, `bigint` per ADR 0002) |
| 3 | ~~**No reverse lookup for reversals.**~~ **Resolved, Phase 6b** by `reversedBy` on `transactionSchema` — present on both `transactions.list` and `transactions.get` | **Resolved 2026-07-28** | Derived per read as the inverse of `reverses_transaction_id`, never stored: a denormalized column would need a backfill, a write-path update, and an invariant keeping the two in agreement. What was actually missing was an **index** — nothing in the first four migrations indexed that column, so the lookup was a sequential scan; migration `0004` adds a partial one (`WHERE reverses_transaction_id IS NOT NULL`, since the column is null for most rows). It is a **list**, not a boolean: nothing forbids reversing twice, so a scalar would be right until the second reversal and silently wrong after. The dialog now states "already been reversed *n* times" instead of disclosing blindness, and ADR 0006 is annotated. **Still open by design:** reversal is still not deduplicated — this made it *visible*, not *illegal*. Also learned: a second reversal is not always possible, since re-applying the mirrored legs can breach invariant #6 and be refused for `insufficient_funds` |
| 4 | ~~**A replayed write is indistinguishable from a fresh one.**~~ **Resolved.** `postedTransactionSchema` now includes `replayed: boolean` on create / reverse / exchange legs. Balances remain "current as of this response" on replay (`ADR 0006`) | **Resolved 2026-08-01** | Console toasts "replayed" vs "posted" on transfer and fee-split. Balances are still live on replay — the flag only answers whether a second posting occurred |
| 5 | **Rate-limit detail is in the response body, not a `Retry-After` header** (`ADR 0007`) | By design | The console reads `scope`, `limit`, and `retryAfterSeconds` from `data`. Nothing needed unless a non-console client appears, which would have to do the same |
| 6 | ~~**`audit.list` has no cursor and caps at 200 entries.**~~ **Resolved, Phase 7a.** Both `audit.list` and `audit.rejections` are cursor-paginated; the screen pages and the "older entries exist but cannot be paged to yet" caveat is gone | **Resolved 2026-07-29** | Ordering is **descending** (most recent first), so the cursor predicate is `<` where every other paginated read here uses `>`. Getting that backwards yields a walk that returns page one forever, which is why both views compose one shared `pageAuditTable` rather than each writing their own. The two tabs page **independently**: `rejections` is a filtered read of the same table, so a cursor taken from the full log and applied to the filtered view would skip rejections — under-reporting on the one screen whose entire job is "what was refused" |
| 7 | ~~**`accounts.list` and `reconciliation.verify` return every account, unpaginated and unfiltered.**~~ **Resolved, Phase 7a**, with two findings worth keeping | **Resolved 2026-07-29** | (1) **The unbounded read had to survive.** `sandbox.reset` calls `listAccounts` four times to plan the chunk that zeroes every balance and then verify none remain; a paged read there would have performed the reset over the first page only and *reported success*. So `listAccounts` stays unbounded for server-side callers and is not reachable from the wire, and `pageAccounts` is the wire read. (2) **Pagination silently weakened five existing tests.** Switching the endpoint immediately broke one sandbox test — and, more instructively, left another *passing* while checking 50 of 105 balances. Assertions about a total, a count, or the *absence* of something cannot read one page; they now walk. (3) `reconciliation.verify`'s verdict is deliberately **not** paginated: `allReconciled`, `accountCount`, and `unreconciledCount` come from a separate whole-org aggregate, because a page-local fold would report a clean ledger while drift sat on page two. Pinned by a test that drifts only the last-sorting account and asserts page one is clean *and* the verdict is false |
| 8 | **There is no `accounts.deactivate`.** An account can be created and can become inactive, but no procedure closes one | Not implemented | The console offers no close action. Add the procedure if closing an account becomes a product requirement |

---

## Frontend testing scope (opened Phase 5a)

| # | Item | Status | Action needed |
|---|---|---|---|
| 9 | **No end-to-end tests.** — **Partially resolved, Phase 6c.** Playwright 1.62 is installed and wired (`apps/web/e2e/`, `pnpm test:e2e`); a real browser now drives a real API + web + Postgres | **Partially resolved 2026-07-28** | **Harness done, coverage deliberately thin: 3 specs.** They cover the auth/tenant gate (unauthenticated → `/login`; sign-up → `/organization` → console) and the empty state. Specs that drove **account creation** and **transfer + reversal** were written, run, and **removed**: the type/account pickers are Base UI `Select` components whose listbox stays mounted after selection, and the specs passed on one run and failed the next. A flaky test is worse than a missing one — it trains people to re-run until green. Those flows remain covered by the `apps/web` component suite and the `packages/api` integration suite; what is missing is specifically *browser-level* proof of them. **Still open:** (a) re-add the write flows once the `Select` interaction has a stable locator strategy, (b) no e2e job in CI — a job that has never been proven in CI should not be added on the strength of a local pass, (c) no viewer-role coverage. The Phase 5 manual demo scripts are **not** retired and remain the record for the uncovered flows |
| 10 | **There is no *working* CI.** ~~Resolved, Phase 6a~~ — **reopened 2026-08-16.** `.github/workflows/ci.yml` is correct and triggers on every push, but **not one run has ever executed a single check.** `gh run list` returns 15 runs from 2026-07-28 to 2026-08-16, every one `failure` in 3–9 seconds, every one annotated: *"The job was not started because your account is locked due to a billing issue."* | **Reopened 2026-08-16 — blocked externally** | **This row previously read "Resolved", and that is the failure worth recording.** A workflow file was written, committed, and marked done without anyone opening the Actions tab; the phase closed on the *existence* of the file rather than on a green run. Every "CI enforces this" claim written since — including #17 below, which debates whether the check is *required* — has been reasoning about a job that has never started. The five commands still pass locally, so nothing is known to be broken; what is missing is any evidence from a machine that is not this laptop. **Nothing in this repo can fix it:** billing is a GitHub account setting on the `Hoverla-Soft` org. Unblock billing, push once, and confirm a green run *before* re-marking this resolved. Until then treat every green check in this repo as local-only | Closed by `.github/workflows/ci.yml`. Two notes: it uses **no Postgres service container** — the db/api suites drive `@testcontainers/postgresql`, which starts its own `postgres:18`, so what they need is a Docker daemon (`ubuntu-latest` has one), and the workflow asserts `docker info` first so a missing daemon fails loudly rather than skipping 260 tests. And it sets throwaway env values explicitly rather than `SKIP_ENV_VALIDATION`, so `packages/env`'s Zod validation stays exercised. **Still open:** branch protection is a repository setting, not a file, so nothing in this repo can assert the check is *required* — see `docs/development/infrastructure.md` |

---

## Documentation and tooling debt (opened Phase 5a)

| # | Item | Status | Action needed |
|---|---|---|---|
| 11 | ~~**`pnpm lint` is documented but does not exist.**~~ **Resolved, Phase 6a** with **Biome 2.5.6** (linter *and* formatter — the repo had neither) | **Resolved 2026-07-28** | Closed by `biome.jsonc` + `"lint": "biome check ."`. Three findings worth keeping: (1) the obvious root `"lint": "turbo run lint"` would have **exited 0 while checking zero files**, since no package defined a `lint` script — the same vacuous-green hazard `no-org-input.test.ts` guards against, which is why the acceptance criteria pin a *file count*; (2) it runs as **one root pass, not a Turborepo fan-out**, because a per-package fan-out cannot reach files belonging to no package — namely `.claude/scripts/*.js`, the guards enforcing this repo's own rules; (3) the config is **`biome.jsonc`** — `biome.json` silently drops config when it contains comments rather than erroring, which here meant linting 551 files including `dist/` while still exiting non-zero. Six `packages/ui` a11y diagnostics are scoped off in an `overrides` block with a per-rule written reason (they misfire on prop-spreading primitives); the same rules stay at full strength in `apps/web` |
| 12 | ~~**`docs/development/work-systems.md` is an unfilled template.**~~ **Resolved 2026-08-16** — filled with the truthful answer, which is that this project has *no* external work systems | **Resolved 2026-08-16** | The row treated "fill it" as conditional on adopting a tracker, but `none` was always the correct entry and CLAUDE.md states reading it as binding — so an unfilled template was a live contradiction, not a deferred chore. Now records: tasks in `docs/tasks/`, docs in `docs/`, design authority in `globals.css` tokens (there is no Figma file), GitHub via `gh` CLI, no MCP connector, no `.mcp.json`. Also states that a session's personal `user`-scope MCP servers are not project authority |
| 12b | **`docs/development/skills-and-plugins.md` is an unfilled template too**, and unlike #12 nobody had recorded it | Not operational | Same fix as #12: fill it with what is actually enabled (`.claude/settings.json` declares the `ponytail` plugin and three hook scripts) or state `none` explicitly |
| 13 | ~~**`packages/ui` contains unused chat-UI scaffolding**~~ **Resolved.** Removed unused chat/form primitives and dropped `@shadcn/react`. Kept `shadcn` for `shadcn/tailwind.css` variants. `Separator` already covers the old marker divider | **Resolved 2026-08-01** | — |
| 14 | ~~**`apps/web` does not extend `packages/config/tsconfig.base.json`.**~~ **Resolved, Phase 6a.** It now extends the shared base, the last of eight workspaces to do so, and `noUncheckedIndexedAccess` is on | **Resolved 2026-07-28** | **The deferral reason recorded here was wrong, and that is the point worth keeping.** This row predicted a wide diff because "cursor stacks, posting arrays, and paginated lists all index". Measured before changing anything: enabling `noUncheckedIndexedAccess` across `apps/web` produced **0 errors over 67 files**, and the full `extends` form produced 0 as well. The probe was confirmed live by planting `const b: string = a[0]` and observing `TS2322`, then again after the change. A cost assumed for a whole phase turned out to be a ten-line config edit — worth measuring before deferring next time. `lib`, `types`, `jsx`, `rootDirs`, `paths` are overridden because the shared base targets Node and this is the only browser workspace |
| 15 | ~~**`packages/ui` emits utility classes that are defined nowhere**~~ **Partially resolved.** Stripped the dead `cn-*` / toast class tokens from the components that still had them. ~~`--font-sans: "Inter Variable"` remains unloaded~~ — **fully resolved 2026-08-16** | **Resolved 2026-08-16** | The font half was closed and the row never updated: `packages/ui/src/styles/globals.css:148` now defines a real platform stack (`ui-sans-serif, system-ui, -apple-system, …`) with the rationale written above it — no webfont, so no flash of unstyled text over a balance. Nothing left to do; the row's advice ("do not invent tokens for missing registry CSS") is what was followed |

---

## Linting and CI (opened Phase 6a)

| # | Item | Status | Action needed |
|---|---|---|---|
| 16 | ~~**`pnpm lint` exits 0 with 26 diagnostics outstanding.**~~ **Resolved 2026-08-16.** All 26 are gone (`2026-08-15-lint-zero-warnings`, commit `441b3fd`): `biome check --diagnostic-level=info .` reports zero diagnostics across 257 files. The row below is kept for the reasoning, which still applies to the next warning anyone adds | **Resolved 2026-08-16** | **Fixing them was only half of it, and the row said so** — "closing this means either fixing them or running `biome check --diagnostic-level=warn`, and the second is only honest once the first is done". The first was done on 2026-08-15 and the second was not, which left a clean state that was a *coincidence* rather than a floor: `biome check .` exits 0 on warnings, so the next `useLiteralKeys` would have landed silently and CI (`ci.yml` runs `pnpm lint` verbatim) would have stayed green. `pnpm lint` is now `biome check --error-on-warnings .`, verified passing at zero diagnostics, so the clean state is enforced rather than observed. Original row: |
| 17 | **CI runs but is not *required*.** `.github/workflows/ci.yml` executes on every push and PR, but branch protection on `main` is a GitHub repository setting, not a file, so nothing in this repo can enforce that a red run blocks a merge | Known limitation | Enable branch protection on `main` requiring the `verify` check. Until then CI reports, it does not gate |
| 19 | ~~**The `apps/web` suite is timing-sensitive and flakes under parallel load.**~~ **Resolved 2026-08-16** by `testTimeout: 15_000` in `apps/web/vitest.config.ts`. Measured first: the slowest test in that file is **1308ms** run in isolation against a 5000ms default, versus the 5351ms recorded here under 4-way `turbo` contention — so the default was measuring machine contention, not correctness. Explicitly **not** closed with `retry`, for the reason the original row gives. |
| 18 | ~~**No dependency, container, or secret scanning.**~~ **Mostly resolved 2026-08-16.** `.github/dependabot.yml` (npm + github-actions) and a `pnpm audit --audit-level=high` step in `ci.yml`. **3 HIGH advisories were live when the gate was added and are now fixed**; 3 moderates + 1 low remain, listed below | **Mostly resolved 2026-08-16** | **The row said an advisory "would currently go unnoticed" — conditional. It was not conditional.** The first `pnpm audit` ever run here returned 9 findings, 3 of them HIGH, including `hono`, which is `apps/server`'s own request framework. Fixed: `hono` 4.12.32→4.13.2 (catalog floor raised to `^4.12.34`, since `^4.12.27` *allowed* the patch but no caret range pulls a fix nobody asks for); `nanoid`→3.3.18 and `brace-expansion`→2.1.4 via `overrides`. **Both overrides need their upper bound**: an open-ended `">=3.3.18"` silently resolved nanoid to 6.x — a different, ESM-only package — and would have broken postcss. **Still open (why the gate is `high`, not `moderate`):** `esbuild` via drizzle-kit's deprecated `@esbuild-kit` chain, `uuid@8.3.2` via `autocannon>hyperid`, and `hono` again via `packages/ui > shadcn`. Each needs a major-version override that breaks its consumer. The `shadcn` one is the tractable one: `packages/ui` carries the **shadcn CLI as a production dependency** — pulling in `@modelcontextprotocol/sdk` and a second `hono` — to serve one line, `@import "shadcn/tailwind.css"` (629 lines of static CSS). Vendoring that file drops the whole subtree. Lower the gate to `moderate` once that list is empty. No SAST or secret scanning yet |

---

## Cross-currency exchange (opened Phase 7c)

| # | Item | Status | Action needed |
|---|---|---|---|
| 20 | **Reversing one leg of an exchange leaves the other standing.** `transactions.reverse` is unchanged and knows nothing about `fx_source_transaction_id`, so reversing the USD half of a USD→EUR exchange restores the USD side and leaves 92.00 EUR sitting in the payee's account, with the EUR bridge still short | Known limitation, deliberate | Not half-solved on purpose. "Reverse the exchange" is a different operation from "reverse this transaction": it needs both legs mirrored under one new idempotency key, and it has to decide what happens when the second mirror is unaffordable — the same trap open question #3 recorded for double reversal. Until then the two legs are individually reversible and the audit log shows exactly what was done. Reversing both by hand is correct and leaves reconciliation clean |
| 21 | **Nothing revalues an FX position or books a gain/loss.** The bridge accounts accumulate offsetting balances (`+100.00 USD` / `-92.00 EUR`) and no process ever marks them to a later rate | Not implemented, out of scope | This is real accounting rather than plumbing, and it needs a rate source to revalue *against* — which the sandbox deliberately does not have (see #22). The positions are at least visible rather than hidden, which is the prerequisite for doing it later |
| 22 | **There is no rate source; the caller states the rate.** Every exchange records whatever rate was supplied, and nothing checks it against a market | By design (`ADR 0010`) | Chosen over a hardcoded rate table (fiction that looks authoritative) and over a provider integration (a dependency, a network boundary, and non-deterministic tests in a fake-money sandbox). The ledger's job here is to record what was agreed. A provider would become worth it only if the sandbox needed to demonstrate a live-rate integration specifically |
| 23 | **A same-currency pair is refused rather than falling back to a plain transfer.** `422 same_currency_exchange` | By design | Accepting it would open a bridge pair in one currency and post two transactions where `transactions.create` posts one. The console's picker excludes same-currency destinations, so this is reachable only by calling the API directly |

---

## Portfolio showcase track (opened 2026-08-01)

| # | Item | Status | Action needed |
|---|---|---|---|
| 24 | **Thin maker-checker for transfers.** Org flag `requireTransferApproval` (default off); `ledger_pending_transfer` holds submissions until a different admin approves/rejects; self-approve/reject returns `403 self_approve_forbidden` | Shipped, **but enforced client-side only** — see #25 | Shipped 2026-08-01 — Approvals nav + org toggle; see `approvals.*` / `settings.*` routers. **The claim "balances move only on approve" was struck on 2026-08-16: it is not true.** See #25 |

---

## Server-side enforcement and hardening (opened 2026-08-16 by the gap audit)

Found by a 12-agent audit whose findings were each adversarially re-verified against the code. Recorded, **not yet fixed** — every row here changes behaviour on a money path and needs a decision before it lands.

| # | Item | Status | Action needed |
|---|---|---|---|
| 25 | **Maker-checker is enforced only in the browser.** `requireTransferApproval` is read in exactly three places — `packages/db/.../org-settings.ts` (storage), `packages/api/src/routers/settings.ts` (get/set), and `apps/web/src/features/transfer/transfer-form.tsx:71`. **`transactions.create` never reads it.** With the flag *on*, any admin posts straight past the approval queue by calling the API directly — curl, a second tab, or the `/api-reference` page this repo publishes | **Open — the control does not do its job** | The whole point of maker-checker is segregation of duties *between admins*, so "an admin can bypass it" is not a lesser threat model, it is the threat model. Two related holes: **fee-split** (`fee-split-form.tsx:73`) calls `transactions.create` and never reads the flag at all, and the transfer form **fails open** — a failed `settings.get` leaves `requireApproval` false and posts immediately. Fix is contained: `approvals.approve` posts via `postTransaction` directly, *not* through the `transactions.create` wire procedure, so a guard inside `create` cannot deadlock the approval path. Adds a new `403 approval_required` to the published contract, so it needs `docs/backend/error-handling.md` and a test alongside |
| 26 | **`approvals.approve` can post the same pending transfer twice.** `packages/api/src/routers/approvals.ts:197` reads the row, checks `status !== "pending"`, posts, *then* marks approved — with a **client-supplied** `idempotencyKey`. Two concurrent approves with different keys both pass the check and both post | **Open — race on a money path** | The idempotency key cannot save this one, because the caller chooses it. Either derive the key from the pending row (`pending.id`) so a second approve replays instead of posting, or take a row lock in the same transaction as the status flip |
| 27 | **A per-amount bound is not a per-balance bound.** `packages/api/src/contracts/money.ts:98` bounds each posting, but nothing bounds the accumulated balance, so an int8 overflow surfaces as an **unaudited 500** rather than a domain rejection | Open | Every other refusal in this ledger is a typed domain error that lands in the audit log. This one is the exception, which makes it the one failure the audit trail cannot explain |
| 28 | **Operational hardening absent in `apps/server` / `createDb`.** No SIGTERM handler (`apps/server/src/index.ts`); the health check is a static string that never touches the database; `createDb` sets no statement timeout, no connection timeout, and no pool bound — while the idempotency reservation is *designed* to block; no request body size limit, so Zod runs only after the whole body is buffered | Open | Low behavioural risk, but the health check is the one that misleads: Railway reads it as "the service is up" while Postgres could be gone |
| 29 | **`approvals.listPending` truncates at 100 rows with no cursor** (`packages/db/src/repositories/pending-transfers.ts:121`) | Open | Same class of bug as #6 and #7, on the one screen whose job is "what is waiting on you". The 101st pending transfer is invisible, not paged |

---

Add a new `## Domain area` section per area rather than one giant table — makes it scannable, and `integration-spec-guard`/`backend-architecture-guard` reference specific sections when they flag something as "should be logged as an open question" instead of assumed.
